[build]
  functions = "netlify/functions"
  publish   = "."

# Redirigir admin.html — solo accesible con token válido (verificado client-side + function)
[[headers]]
  for = "/admin.html"
  [headers.values]
    X-Robots-Tag = "noindex, nofollow"
    Cache-Control = "no-store"
